from setuptools import setup

setup(name='tv_models',
      version='0.0.1',
      description='',
      url='',
      author='',
      requires=[
          'bs4',
          'natsort', 'requests',
      ])
